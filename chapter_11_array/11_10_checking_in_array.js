let arr = Array.isArray([1,2,3,4])
console.log(arr)

let ac = Array.isArray("a")
console.log(ac);


[80,90,100].every(abc => abc >50); // true // every is used to check all the elements should be 

[80,90,60].some(xyz => xyz <70) //true  


//api playwight
[200,201,203].every(pop => pop >=200)